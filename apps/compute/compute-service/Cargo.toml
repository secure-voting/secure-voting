[package]
categories = [
  "asynchronous",
  "network-programming",
  "web-programming::http"
]
description = "An RPC-service capable of electing a winner in a ranked-ballot voting configuration"
edition = "2024"
keywords = [
  "election",
  "rpc",
  "service",
  "voting"
]
license.workspace=true
name = "compute-service"
repository = "https://github.com/schukark/secure-voting/apps/compute/compute-service"
version = "0.1.0"

[dependencies]

[lints]
workspace=true

[package.metadata.clippy]
config-file = "../../clippy.toml"
